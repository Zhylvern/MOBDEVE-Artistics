package com.example.mobdeveartistics.models

class SongRow(songImage: Int, songTitle: String, songArtist: String, songNumPlays: String, songDuration: String) {

    var songImage = songImage
        private set

    var songTitle = songTitle
        private set

    var songArtist = songArtist
        private set

    var songNumPlays = songNumPlays
        private set

    var songDuration = songDuration
        private set
}